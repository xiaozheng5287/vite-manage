## 数组理论基础
# 数组是存放在连续内存空间上的相同类型数据的集合。
# 数组在内存空间的地址是连续的，所以我们在删除或者增添元素的时候，就难免要移动其他元素的地址。
# 数组的元素是不能删的，只能覆盖。


## 内存地址
# 内存地址是计算机内存的物理地址，是物理地址的编号，是物理地址的标识符。
# int型的数组，两个相邻数组元素地址差4个字节。

https://programmercarl.com/0704.%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE.html#%E6%80%9D%E8%B7%AF
## 二分查找
# 二分查找的前提是数组是有序的，且无重复元素
# 二分查找的时间复杂度是O(logn)
# 二分查找的思路是：每次取中间的元素和目标值进行比较，如果中间的元素大于目标值，那么目标值在左半部分，如果中间的元素小于目标值，那么目标值在右半部分，如果中间的元素等于目标值，那么就找到了目标值。

## 位运算
# 位运算（Bit Operation）是指直接对整数的二进制位进行操作的运算。在计算机内部，所有的数据都是以二进制形式存储的，因此位运算可以直接操作这些二进制位，从而实现一些高效的计算。
